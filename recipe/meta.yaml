{% set name = "pycvodes" %}
{% set version = "0.14.7" %}
{% set sha256 = "8671e524a1fca4b310e333cd0dbc5648a2a40aef6db45b8d3087e66c6f2ce1ca" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: true  # [win and not py>=35]
  features:

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
  host:
    - libblas
    - liblapack
    - sundials <7
    - python
    - setuptools
    - pip
    - numpy
    - cython
  run:
    - sundials <7
    - python
    - setuptools  # pkg_resources used

test:
  imports:
    - pycvodes
  requires:
    - pytest
  commands:
    - python -m pytest --pyargs {{ name }}
    - python -c "import pathlib as p; from pycvodes import get_include as g; assert open(p.Path(g()) / 'cvodes_cxx.hpp', 'rt').read().startswith('#pragma once')"

about:
  home: https://github.com/bjodah/{{ name }}
  license: BSD-2-Clause
  license_file: LICENSE
  summary: Python wrapper around cvodes (from the sundials library)
  doc_url: https://bjodah.github.io/{{ name }}/latest


extra:
  recipe-maintainers:
    - bjodah
    - moorepants
